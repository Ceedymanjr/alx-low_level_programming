#include "dog.h"
#include <stdio.h>
/**
 * _strlen - function that find the lenght of the string
 * @s: string to find the lenght
 * Return: Always 0
 */
int _strlen(char *s)
{
	int i = 0;

	while (s[i] != '\0')
		i++;
	return (0);
}


int *_strcpy(char *dest, *src)
