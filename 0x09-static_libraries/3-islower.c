#include "main.h"
/**
 * _islower - function that check lower case
 *
 * @c: function pparameter
 *
 * Return: return 1 if is lower
 * and 0 otherwise
 */

int _islower(int c)
{
	if
		(c >= 97 && c <= 122)
	{
		return (1);
	}
	else
		return (0);
}
